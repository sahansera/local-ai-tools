{
  "name": "@local-ai-tools/catalog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "tsx src/cli.ts generate",
    "validate": "tsx src/cli.ts validate",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@local-ai-tools/schema": "workspace:*",
    "yaml": "^2.8.1"
  }
}
